aws Key words:

```
ARN  = ARN stands for Amazon Resource Name
    > It is a unique identifier assigned to each resource in AWS
    > It helps you clearly identify any resource across various AWS services
    > ARNs are required in situations where you need to pinpoint a specific resource
EX: arn:aws:iam::891376921430:user/test-user


```
